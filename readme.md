# Front-end - React Page Builder

A React-based page builder for the Ciob/Fc project using TanStack Start, Supabase, and Cloudflare.

## Tech Stack

- **Framework**: TanStack Start (React 19)
- **Database**: Supabase (PostgreSQL)
- **Styling**: Tailwind CSS 4
- **Deployment**: Cloudflare Pages

## Getting Started

1. Install dependencies:

   ```bash
   npm install
   ```

2. Copy environment variables:

   ```bash
   cp .env.example .env
   ```

   Fill in your Supabase values in `.env`

3. Run development server:
   ```bash
   npm run dev
   ```

## Available Scripts

- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm run preview` - Preview production build
- `npm run lint` - Run ESLint

## Supabase

- Database: https://supabase.com/dashboard/[project]
- Docs: https://supabase.com/docs
